# AIoT 智能设备管理平台 — 阿里云容器化部署指南

本文档说明如何在 **阿里云 ECS** 上，从远程 Git 仓库拉取代码并完成 **Docker Compose 一键容器化部署**。

> 适用仓库：`https://github.com/Jadefjg/aiot-sys.git`  
> 部署模式：**单体全栈**（根目录 `docker-compose.yml`）  
> 旧版非容器部署说明见 `DEPLOYMENT.md`（systemd + 宿主机 Nginx），新项目请优先使用本文档。

---

## 1. 推荐方案概览

| 方案 | 说明 | 推荐度 |
|------|------|--------|
| **A. ECS 上 clone + compose build** | 代码已在 GitHub，服务器直接拉取并构建 | ⭐⭐⭐ 首选 |
| B. 本地 build → 推送 ACR → ECS pull | 适合 CI/CD、多环境版本管理 | ⭐⭐ 进阶 |
| C. docker save/load 离线包 | 仅临时迁移或内网环境 | ⭐ 不推荐 |

**最佳实践：方案 A** — 维护简单、与仓库结构一致、无需维护镜像仓库；后续可平滑升级到方案 B。

### 1.1 容器架构

```
                    公网访问
                       │
                       ▼
            ┌──────────────────────┐
            │  frontend_app (Nginx) │  :80 / :8080
            │  /iot/ 前端 SPA       │
            │  /api/  反向代理      │
            └──────────┬───────────┘
                       │
     ┌─────────────────┼─────────────────┐
     ▼                 ▼                 ▼
 backend_app    celery_worker      influxdb
 (FastAPI)       (固件异步任务)      (时序数据)
     │
     ├── mysql_db
     ├── redis_cache
     └── mqtt_broker (EMQX)
```

### 1.2 服务清单

| 容器 | 镜像/构建 | 对外端口（默认） | 说明 |
|------|-----------|------------------|------|
| frontend_app | 构建 `iot_web` | `HTTP_PORT`（建议 80） | Nginx + Vue3 |
| backend_app | 构建 `iot_backend` | 无（经 Nginx 代理） | FastAPI |
| celery_worker | 同 backend | 无 | Celery Worker |
| mysql_db | mysql:8.0 | 3306（生产勿公网开放） | 业务库 |
| redis_cache | redis:7-alpine | 6379（生产勿公网开放） | 缓存/队列 |
| mqtt_broker | emqx/emqx:5.3.0 | 1883, 18083, 18084 | MQTT |
| influxdb | influxdb:2.7 | 8086（按需） | 遥测存储 |

---

## 2. 阿里云资源准备

### 2.1 ECS 规格建议

| 环境 | CPU | 内存 | 系统盘 | 操作系统 |
|------|-----|------|--------|----------|
| 测试 | 2 核 | 4 GB | 40 GB | Alibaba Cloud Linux 3 / Ubuntu 22.04 |
| 生产 | 4 核 | 8 GB | 80 GB+ | 同上 |

### 2.2 安全组（入方向）

| 端口 | 协议 | 用途 | 是否必须 |
|------|------|------|----------|
| 22 | TCP | SSH | 是 |
| 80 | TCP | Web 控制台（`HTTP_PORT=80`） | 是 |
| 8080 | TCP | Web（若 `HTTP_PORT=8080`） | 二选一 |
| 1883 | TCP | MQTT 设备接入 | 按需 |
| 18083 | TCP | EMQX Dashboard | 按需（建议限制来源 IP） |
| 443 | TCP | HTTPS（域名 + 证书） | 推荐 |

**不要**对 `0.0.0.0/0` 开放 **3306、6379、8086**，数据库与 Redis 仅容器内网访问即可。

### 2.3 安装 Docker 与 Compose

以 **Alibaba Cloud Linux 3 / CentOS 系** 为例：

```bash
# 更新系统
sudo yum update -y

# 安装 Docker（官方脚本）
curl -fsSL https://get.docker.com | sudo sh
sudo systemctl enable docker
sudo systemctl start docker

# 安装 Compose 插件
sudo yum install -y docker-compose-plugin

# 验证
docker --version
docker compose version
```

Ubuntu 可使用：

```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin
sudo systemctl enable docker && sudo systemctl start docker
```

### 2.4 配置 Docker 镜像加速（国内必做）

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.xuanyuan.me"
  ],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "50m",
    "max-file": "3"
  }
}
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
```

---

## 3. 首次部署（方案 A — 推荐）

以下命令均在 **ECS 上** 执行。将 `<ECS公网IP>` 替换为实际 IP 或域名。

### 3.1 拉取代码

```bash
sudo mkdir -p /opt/aiot-sys
sudo chown "$USER":"$USER" /opt/aiot-sys
cd /opt/aiot-sys

git clone https://github.com/Jadefjg/aiot-sys.git .
# 若需指定分支：git checkout master
```

### 3.2 配置生产环境变量

```bash
cp .env.example .env
vim .env   # 或 nano .env
```

**生产环境必改项**（示例，请自行生成强密码）：

```bash
# Web 入口端口（安全组需对应放行）
HTTP_PORT=80

# 数据库 — 务必修改
MYSQL_ROOT_PASSWORD=<强密码>
MYSQL_PASSWORD=<强密码>

# JWT — 务必修改（可用: openssl rand -hex 32）
SECRET_KEY=<随机长字符串>

# 跨域与固件下载地址 — 改为公网访问地址
CORS_ORIGINS=http://<ECS公网IP>,http://<你的域名>
FIRMWARE_BASE_URL=http://<ECS公网IP>/firmware_files

# InfluxDB — 生产建议修改
INFLUX_PASSWORD=<强密码>
INFLUX_TOKEN=<随机 token>

# 可选
PROJECT_NAME=AIoT 智能设备管理平台
ACCESS_TOKEN_EXPIRE_MINUTES=1440
```

> 使用域名时，将 `<ECS公网IP>` 换成 `https://your.domain.com`，并确保 `CORS_ORIGINS` 含完整协议。

### 3.3 构建并启动

```bash
cd /opt/aiot-sys
docker compose up -d --build
```

首次启动约 3～8 分钟（拉基础镜像 + 构建前后端）。查看进度：

```bash
docker compose ps
docker compose logs -f backend_app
```

全部 `healthy` / `running` 后，后端 entrypoint 会自动执行：

- 等待 MySQL 就绪
- 初始化默认用户（`scripts/seed_users.py`）
- 初始化示例产品（`scripts/seed_products.py`）

### 3.4 验证部署

```bash
# 健康检查
curl -fsS http://127.0.0.1/health && echo " OK"

# 容器状态
docker compose ps

# 登录 API 冒烟测试
curl -s -X POST http://127.0.0.1/api/v1/auth/login \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'username=admin&password=admin123'
```

浏览器访问：

| 服务 | 地址 |
|------|------|
| **控制台** | `http://<ECS公网IP>/iot/` |
| API 文档 | `http://<ECS公网IP>/docs` |
| 健康检查 | `http://<ECS公网IP>/health` |
| EMQX 控制台 | `http://<ECS公网IP>:18083` |

**默认登录账号**（首次 seed 后）：

| 系统 | 用户名 | 密码 |
|------|--------|------|
| IoT 控制台 | admin | admin123 |
| EMQX | admin | public |

> ⚠️ 上线后请立即在控制台修改 admin 密码，并修改 EMQX 默认密码。

---

## 4. 日常运维

### 4.1 常用命令

```bash
cd /opt/aiot-sys

# 查看状态
docker compose ps

# 查看日志
docker compose logs -f backend_app
docker compose logs -f frontend_app
docker compose logs -f celery_worker

# 重启单个服务
docker compose restart backend_app

# 停止（保留数据卷）
docker compose down

# 停止并清空所有数据（慎用！）
docker compose down -v
```

### 4.2 更新版本

```bash
cd /opt/aiot-sys
git pull origin master
docker compose up -d --build
```

更新后若浏览器仍报「动态模块加载失败」，对 `/iot/login` 执行 **强制刷新**（Ctrl+Shift+R / Cmd+Shift+R）。

### 4.3 数据备份

```bash
# MySQL 备份
docker compose exec -T mysql_db mysqldump -u iot_user -p'<MYSQL_PASSWORD>' iot_db \
  > backup_iot_db_$(date +%F).sql

# 固件文件卷（可选）
docker run --rm -v aiot-sys_firmware_storage:/data -v $(pwd):/backup \
  alpine tar czf /backup/firmware_$(date +%F).tar.gz -C /data .
```

建议配合阿里云 **快照/对象存储 OSS** 做定期备份。

### 4.4 资源与磁盘

```bash
docker system df
docker image prune -f    # 清理悬空镜像
```

---

## 5. 进阶：ACR 镜像仓库部署（方案 B）

适合团队 CI/CD、固定版本回滚。流程：**本地或 CI 构建 → 推送阿里云 ACR → ECS 只 pull 不 build**。

### 5.1 创建 ACR

1. 登录 [容器镜像服务 ACR](https://cr.console.aliyun.com/)
2. 创建命名空间，如 `aiot`
3. 创建镜像仓库：`aiot-backend`、`aiot-frontend`

### 5.2 本地构建并推送（Mac M 芯片推 x86 服务器时）

```bash
REGISTRY=registry.cn-hangzhou.aliyuncs.com/<命名空间>
TAG=v1.0.0

# 登录
docker login --username=<阿里云账号> $REGISTRY

# 在仓库根目录构建（指定 amd64）
docker buildx build --platform linux/amd64 \
  -t $REGISTRY/aiot-backend:$TAG --push ./iot_backend

docker buildx build --platform linux/amd64 \
  -t $REGISTRY/aiot-frontend:$TAG --push ./iot_web
```

### 5.3 编写生产 compose 覆盖文件

创建 `docker-compose.prod.yml`：

```yaml
services:
  backend_app:
    image: registry.cn-hangzhou.aliyuncs.com/<命名空间>/aiot-backend:v1.0.0
    build: !override null

  celery_worker:
    image: registry.cn-hangzhou.aliyuncs.com/<命名空间>/aiot-backend:v1.0.0
    build: !override null

  frontend_app:
    image: registry.cn-hangzhou.aliyuncs.com/<命名空间>/aiot-frontend:v1.0.0
    build: !override null
```

ECS 部署：

```bash
docker login --username=<阿里云账号> registry.cn-hangzhou.aliyuncs.com
docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

---

## 6. HTTPS 与域名（可选）

当前 `frontend_app` 已内置 Nginx。生产环境推荐：

1. 域名 A 记录指向 ECS 公网 IP  
2. 安全组放行 443  
3. 任选其一：
   - **阿里云 SSL + SLB/ALB** 终止 HTTPS，回源 80  
   - 在 ECS 宿主机再装一层 Nginx/Certbot 做 443 → 容器 80 转发  
   - 修改 `iot_web/nginx.docker.conf` 增加 443 与证书挂载（需重建 frontend 镜像）

启用 HTTPS 后，同步更新 `.env`：

```bash
CORS_ORIGINS=https://your.domain.com
FIRMWARE_BASE_URL=https://your.domain.com/firmware_files
```

---

## 7. 故障排查

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| 无法访问 `/iot/` | 安全组未放行 80/8080 | 检查安全组与 `HTTP_PORT` |
| 登录成功但白屏/控制台报 chunk 404 | 浏览器缓存旧 index.html | 强制刷新；确认 `index.html` 无缓存 |
| `backend_app`  unhealthy | MySQL 未就绪或密码错误 | `docker compose logs backend_app` |
| API 502 | 后端尚未启动完成 | 等待 healthcheck；`docker compose ps` |
| MQTT 连不上 | 1883 未放行 | 安全组添加 1883/TCP |
| `git pull` 后行为异常 | 未 rebuild | `docker compose up -d --build` |

```bash
# 一键诊断
cd /opt/aiot-sys
docker compose ps
curl -v http://127.0.0.1/health
curl -v http://127.0.0.1/iot/index.html | head
docker compose logs --tail=100 backend_app
```

---

## 8. 安全 checklist

- [ ] 修改 `MYSQL_ROOT_PASSWORD`、`MYSQL_PASSWORD`、`SECRET_KEY`
- [ ] 修改 InfluxDB 密码与 Token
- [ ] 修改 IoT 控制台 admin 默认密码
- [ ] 修改 EMQX 默认密码（18083）
- [ ] 安全组不暴露 3306 / 6379 / 8086
- [ ] SSH 使用密钥登录，禁用 root 密码登录（推荐）
- [ ] 定期 `git pull` + 备份数据库
- [ ] 有域名时启用 HTTPS

---

## 9. 快速命令备忘

```bash
# === 首次部署 ===
cd /opt/aiot-sys
git clone https://github.com/Jadefjg/aiot-sys.git .
cp .env.example .env && vim .env
docker compose up -d --build
docker compose ps

# === 访问 ===
# 控制台  http://<IP>/iot/
# API     http://<IP>/api/v1/
# 文档    http://<IP>/docs
# EMQX    http://<IP>:18083
# 账号    admin / admin123

# === 更新 ===
git pull && docker compose up -d --build

# === 日志 ===
docker compose logs -f backend_app
```

---

## 10. 相关文件

| 文件 | 说明 |
|------|------|
| `docker-compose.yml` | 单体全栈编排 |
| `.env.example` | 环境变量模板 |
| `iot_backend/Dockerfile` | 后端镜像 |
| `iot_backend/docker-entrypoint.sh` | 启动等待 MySQL + 种子数据 |
| `iot_web/Dockerfile` | 前端构建 + Nginx |
| `iot_web/nginx.docker.conf` | 前端路由与 API 反代 |
| `DEPLOYMENT.md` | 旧版宿主机部署（非容器） |

---

*文档版本：与仓库 `master` 分支 docker-compose 单体部署同步。*
